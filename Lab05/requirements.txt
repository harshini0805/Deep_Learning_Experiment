# Experiment 5 — MobileNetV2 on Oxford-IIIT Pet
# Python 3.10–3.12 (developed on Python 3.12.5)

# Deep learning (Keras 3 ships with TF >= 2.16; the notebook uses tf.keras + Keras 3 APIs)
tensorflow>=2.16,<2.22
# Linux / WSL2 with an NVIDIA GPU: replace the line above with
# tensorflow[and-cuda]>=2.16,<2.22

# Numerics & data
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
pillow>=10.0

# Plotting
matplotlib>=3.7
seaborn>=0.13

# Notebook runtime
jupyter>=1.0
ipykernel>=6.25
